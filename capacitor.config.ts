import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.aaeca.aaeca',
  appName: 'aaeca',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
